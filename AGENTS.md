# AGENTS.md

本文件约束 Codex 在本仓库中编写、修改和审查 SystemVerilog（系统级硬件描述与验证语言）代码时的行为。除非用户在当前任务中明确覆盖某条规则，否则以下规则均为强制要求。

## 1. 规格优先级

1. 每个练习目录中的 `README.md` 是该练习唯一有效的功能规格来源。
2. 必须严格遵守 `README.md` 中冻结的模块名、参数、端口、位宽、时序语义、复位语义、边界条件和验收要求。
3. 严禁为了实现方便而新增、删除、重命名或改变端口方向与位宽。
4. 若规格存在歧义，不得自行扩展功能；应优先采用最小实现，并在提交说明中明确指出歧义。
5. 根目录约定与单题 `README.md` 冲突时，以单题 `README.md` 为准；用户当前任务中的明确要求优先级最高。

## 2. 目录边界

每个练习目录遵循以下结构：

```text
exercises/<exercise>/
├── README.md
├── golden/
└── practice/
```

- `golden/`：Codex 编写参考实现、参考测试平台和必要说明的目录。
- `practice/`：练习者本人编写代码的目录。
- 除非用户明确要求，Codex **不得修改、补全、格式化、重构或覆盖 `practice/` 中的任何 `.sv` 文件**。
- Codex 默认只在 `golden/` 中生成参考答案。

## 3. 严禁使用 `function`

这是本仓库的最高优先级编码约束之一。

### 3.1 绝对禁止

任何 `.sv` 文件中均严禁：

- 定义 `function`；
- 定义 `automatic function`；
- 在 `module`、`interface`、`package`、`class` 或其他作用域中定义函数；
- 调用自定义 SystemVerilog 函数；
- 为了参数计算、编码转换、优先级选择、格雷码转换、地址计算、比较、饱和、裁剪等逻辑引入辅助函数。

例如，以下写法在本仓库中 **禁止**：

```systemverilog
function automatic logic [PTR_W-1:0] bin2gray(input logic [PTR_W-1:0] bin);
    bin2gray = (bin >> 1) ^ bin;
endfunction
```

必须改为直接使用清晰的组合表达式或 `always_comb`（组合逻辑过程块），例如：

```systemverilog
gray_next = (bin_next >> 1) ^ bin_next;
```

### 3.2 对测试平台同样生效

`golden/` 中的测试平台也不得使用 `function`。如果需要复用测试流程，可在测试平台中使用少量、清晰的 `task`（任务），但不得用 `task` 隐藏被测设计核心行为或参考模型的关键逻辑。

## 4. RTL 编码风格

### 4.1 可综合性

参考 RTL（寄存器传输级）实现必须可综合，且不得依赖厂商专有 IP（知识产权核）。

综合 RTL 中禁止：

- `#` 延时；
- `initial` 用于功能实现；
- `force/release`；
- `fork/join`；
- 动态数组、队列、关联数组；
- class（类）等仅验证用途结构；
- DPI（直接编程接口）；
- 不可综合系统任务参与功能逻辑。

### 4.2 时序逻辑

- 时序逻辑优先使用 `always_ff`（时序逻辑过程块）。
- 时序赋值使用非阻塞赋值 `<=`。
- 复位类型和有效电平必须严格遵守单题规格。
- 同一个寄存器不得被多个过程块驱动。
- 不得依赖仿真调度竞争获得正确结果。

### 4.3 组合逻辑

- 组合逻辑优先使用 `always_comb`（组合逻辑过程块）或连续赋值 `assign`。
- `always_comb` 内使用阻塞赋值 `=`。
- 所有组合输出和中间变量必须在所有路径上有确定赋值，严禁无意推导 latch（锁存器）。
- 优先在过程块开头给默认值，再覆盖特定条件。
- 避免不必要的深层嵌套和长优先级链。

### 4.4 一个信号一个驱动源

- 一个 `logic` 信号只能有一个明确驱动源。
- 严禁同时由 `assign` 和 `always_*` 驱动同一信号。
- 严禁多个 `always_ff` 或 `always_comb` 驱动同一变量。

## 5. 面试友好原则

本仓库用于 RTL 手撕练习，参考答案必须优先考虑“面试现场可解释、可复现、可手写”，而不是追求炫技。

必须遵守：

- 优先采用直接、清晰、常见的硬件结构；
- 不使用晦涩宏技巧掩盖核心逻辑；
- 不使用过度抽象；
- 不为了减少代码行数牺牲可读性；
- 不用复杂语言特性代替应当显式展示的硬件思路；
- 关键状态、计数器、指针、握手条件应显式可见；
- 对 FIFO（先进先出队列）、CDC（跨时钟域）、仲裁器、流水线等题，代码应能直接对应其硬件原理。

## 6. 参数化约束

- 只对规格中明确要求参数化的部分进行参数化。
- 不为“通用性”额外增加规格之外的参数。
- 参数表达式应简单、可综合、可读。
- 可以使用 `$clog2` 等标准系统函数进行静态位宽计算；本文件禁止的是用户自定义 `function`，不是标准 SystemVerilog 系统函数。
- 若规格要求支持非 2 的整数次幂深度，必须显式处理回绕或边界，不能依赖自然二进制溢出。

## 7. FIFO、握手和仲裁的通用要求

除非单题规格另有定义：

- 状态只在操作真正成功时更新。
- 对 valid-ready（有效-就绪）接口，传输只在 `valid && ready` 成立时发生。
- 当 `valid=1 && ready=0` 时，发送端有效数据必须保持稳定。
- FIFO 的读写指针只能在对应读写操作成功时移动。
- 仲裁器状态应在请求真正完成服务后更新，而不是仅仅产生 grant（授权）时更新。
- 必须显式处理满、空、同时读写、回绕和边界周期。

## 8. CDC 约束

CDC（跨时钟域）题必须体现正确的跨域结构，不得只为仿真通过而直接跨域采样。

- 单比特电平同步按规格使用多级同步触发器。
- 异步 FIFO 中跨域指针应按规格使用 Gray Code（格雷码）等安全编码。
- 格雷码转换必须直接写表达式或组合逻辑，**不得封装为 `function`**。
- 不得对普通多比特总线逐位简单套两级同步器后假定其整体一致。
- 跨时钟域同步寄存器应保持结构清楚，便于面试解释。

## 9. 测试平台要求

Codex 为 `golden/` 编写测试平台时：

- 测试平台应自检，不只打印波形供人工观察。
- 应覆盖 `README.md` 明确列出的边界条件和验收重点。
- 失败时应给出明确的错误信息。
- 测试平台可使用 `initial`、时钟生成、`$display`、`$fatal` 等验证手段。
- 测试平台不得修改被测模块接口。
- 测试平台不得通过层次化强制修改被测模块内部状态来伪造通过结果。
- 测试平台同样严禁使用 `function`。
- 如需复用激励步骤，可使用简单 `task`，但保持测试逻辑易读。

## 10. Golden 参考答案文件约定

默认建议每个 `golden/` 至少包含：

```text
golden/
├── <module_name>.sv
└── tb_<module_name>.sv
```

若题目确实需要多个 RTL 文件，可以拆分，但应保持文件数量最少，不得无意义拆模块。

参考实现应：

- 模块名与规格完全一致；
- 文件名清晰；
- 顶层端口完全匹配规格；
- 不增加调试端口；
- 不加入与题目无关的额外功能；
- 不在代码中留下未完成的 TODO（待办事项）；
- 不使用 `function`。

## 11. 提交前强制自检

Codex 在提交或宣称完成前，必须逐项确认：

1. 是否读完当前练习的 `README.md`；
2. 端口是否逐项匹配冻结规格；
3. 是否错误修改了 `practice/`；
4. 是否存在任何 `function` 关键字或自定义函数调用；
5. RTL 是否可综合；
6. 是否存在无意锁存器或多驱动；
7. 时序逻辑是否使用非阻塞赋值；
8. 组合逻辑是否覆盖所有赋值路径；
9. 边界条件是否全部处理；
10. 测试平台是否真正检查功能而不是只生成波形；
11. 是否为了方便私自改变题目语义；
12. 是否能够用简单硬件结构解释当前实现。

若任一项不满足，不得将任务标记为完成。

## 12. 最重要的三条

如果只能记住三条，必须遵守：

1. **严格按照每题 `README.md` 的冻结规格和最小端口实现。**
2. **任何 `.sv` 文件都严禁定义或使用自定义 `function`。**
3. **除非用户明确要求，绝不修改 `practice/`；Codex 的参考答案只写入 `golden/`。**
